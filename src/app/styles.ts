import { colors } from '@/styles/global';
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  backgroundStyle: {
    backgroundColor: colors.gray700.color,
    flex: 1,
    alignItems: 'center',
  },
});

export const { backgroundStyle } = styles;
